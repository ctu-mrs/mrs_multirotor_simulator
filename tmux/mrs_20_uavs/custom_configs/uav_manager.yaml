takeoff:

  during_takeoff:
    controller: "Se3Controller"
    tracker: "LandoffTracker"

  after_takeoff:
    controller: "Se3Controller"
    tracker: "MpcTracker"

min_height_checking:
  enabled: false

max_height_checking:
  enabled: false
